<%= render "navbar" %>

<h1> Users </h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.email, user %>
    </li>
  <% end %>
</ul>

<%= link_to "New User", new_user_path, class: "btn btn-success" %>
